# AI视频水印去除模型开发提示词

## 基础提示词

你是一位专业的AI模型开发工程师，专注于视频处理和计算机视觉领域。请为我开发一个用于去除视频水印的AI模型，具体要求如下：

1. **模型目标**：开发一个能够自动检测并去除视频中水印的AI模型
2. **技术栈**：使用深度学习框架（如PyTorch、TensorFlow等）
3. **模型架构**：设计一个端到端的视频处理模型，能够处理连续帧的水印去除
4. **训练数据**：说明需要的训练数据类型和准备方法
5. **评估指标**：定义模型性能的评估标准
6. **实现步骤**：提供详细的开发和训练步骤
7. **部署方案**：说明如何将模型部署到实际应用中

## 高级提示词

### 技术要求
- 使用PyTorch框架进行开发
- 设计一个基于U-Net的编码器-解码器架构，结合注意力机制
- 实现3D卷积或时间序列模块来处理视频的时间连续性
- 使用混合损失函数：像素级MSE损失 + 感知损失 + 对抗损失
- 采用AdamW优化器，学习率使用余弦退火策略

### 数据处理
- 收集至少1000个带有水印的视频片段和对应的无水印版本
- 合成数据集：在无水印视频上添加各种类型的人工水印（文本、Logo、半透明等）
- 数据增强：随机裁剪、旋转、缩放、亮度/对比度调整、高斯噪声添加
- 帧提取：每秒提取25帧，每段视频使用连续16帧作为输入

### 模型训练
- 初始预训练：使用图像水印去除数据集进行预训练
- 视频数据集微调：使用提取的视频帧进行微调
- 训练批次大小：8（根据GPU内存调整）
- 训练轮次：200轮，每20轮保存一次模型
- 验证频率：每5轮进行一次验证

### 性能要求
- PSNR目标值：≥35dB
- SSIM目标值：≥0.95
- 推理速度：≥30FPS（在GPU上）
- 支持实时视频处理

### 鲁棒性要求
- 能够处理不同类型的水印（文本、Logo、半透明等）
- 能够处理不同位置的水印（角落、中心、全屏等）
- 能够处理不同强度的水印（弱、中、强）
- 保持原始视频的视觉质量和连续性

### 实现细节
- 提供完整的代码实现，包括模型定义、数据加载、训练循环、评估脚本
- 包含详细的注释和文档
- 提供模型训练和推理的命令行接口
- 支持模型导出为ONNX格式，便于部署

### 部署方案
- 提供Python推理脚本
- 支持视频文件和实时摄像头输入
- 提供简单的命令行界面
- 说明如何集成到其他应用程序中

## 示例代码结构

请按照以下项目结构组织代码：

```
/video-watermark-removal/
├── data/                 # 数据集
│   ├── raw/             # 原始数据
│   ├── processed/       # 处理后数据
│   └── synthetic/       # 合成数据
├── models/              # 模型定义
│   ├── architectures/   # 网络架构
│   └── pretrained/      # 预训练模型
├── training/            # 训练相关
│   ├── train.py         # 训练脚本
│   ├── evaluate.py      # 评估脚本
│   └── configs/         # 配置文件
├── inference/           # 推理相关
│   ├── infer.py         # 推理脚本
│   └── demo.py          # 演示脚本
├── utils/               # 工具函数
│   ├── data.py          # 数据处理
│   ├── metrics.py       # 评估指标
│   └── visualization.py # 可视化工具
├── requirements.txt     # 依赖文件
└── README.md            # 项目说明
```

## 预期成果

- 一个能够有效去除视频水印的AI模型
- 完整的模型开发和训练代码
- 详细的文档和使用说明
- 性能评估报告
- 可部署的推理脚本
